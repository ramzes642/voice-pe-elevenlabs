# Copy to .env (gitignored) and fill in. Never commit the real .env.
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=

# Phase 2+ (Home Assistant / tools)
# HASS_URL=
# HASS_TOKEN=
# WOLT_MCP_CMD=uv run --directory /path/to/wolt wolt-mcp
